package DBoW2.proto;
import "orb_node.proto";
import "orb_word.proto";

message ORBVocabulary {
  optional int32 k = 1;
  optional int32 l = 2;
  optional int32 scoring_type = 3;
  optional int32 weighting_type = 4;
  repeated ORBNode nodes = 5;
  repeated ORBWord words = 6;
}